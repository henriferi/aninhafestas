export interface SpaceSlide {
  id: string;
  title: string;
  description: string;
  image: string;
  features: string[];
}

export interface Equipment {
  id: string;
  name: string;
  description: string;
  image: string;
  ageRange: string;
  capacity: string;
  features: string[];
}